import './styles.css'
import { AppBar, Button, IconButton, Stack, Toolbar, Typography } from "@mui/material";

export const Menubar=()=>{
    return (
        <AppBar>
            <Toolbar>

            </Toolbar>
        </AppBar>
    )
}